'use strict';
window.onload = function() {
  setupVisitorCount();
};

// ----------------------------------------------------------------------------
// Visitor count set-up
// ----------------------------------------------------------------------------

/**
 * Sets-up the visitor counter
 */

function setupVisitorCount() {}
